Increasing:

{% for i in range(0, 3) %}
    {{ i }}
    {% if not loop.last %}
        ,
    {% endif %}
{% endfor %}

<br>

Increasing with steps:

{% for i in range(0, 10, 2) %}
    {{ i }}
    {% if not loop.last %}
        ,
    {% endif %}
{% endfor %}

<br>

Decreasing with steps:

{% for i in range(10, 0, -2) %}
    {{ i }}
    {% if not loop.last %}
        ,
    {% endif %}
{% endfor %}

<br>
